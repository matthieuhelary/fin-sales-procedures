# fin-sales-procedures

Guide d’entretien ADV / commerciaux — procédures Fin (Intercom).

Static site (`index.html`) + `robots.txt` (`Disallow: /`, noindex).

Deployed on Vercel for sharing with Intercom (public URL, not indexed by search engines).
